<!--component/customTabBar/customTabBar.wxml-->
<view class="tabbar">
  <view
    wx:for="{{list}}"
    wx:key="index"
    data-path="{{item.pagePath}}"
    data-index="{{index}}"
    bindtap="switchTab"
  >
    <iconfont name="{{item.iconName}}" size="64"></iconfont>
  </view>
</view>
